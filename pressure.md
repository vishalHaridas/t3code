## CURRENT

- Search currently structured-clones full selected thread snapshots into a worker on each submit.
- Search still recomputes tokenization from scratch for every query.
- Ask still sends complete selected source text and depends on prompt-size blocking instead of indexed retrieval.

## RECENT

- Web Worker search avoids UI freezes but does not remove repeated corpus scan cost.

## ARCHIVE
